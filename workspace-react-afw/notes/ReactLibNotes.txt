1. create tgz of lib
	a. workspace-react-afw/afw-components>npm run build:pack
2. move it to target project's /lib and install
	a. workspace-react-afw/demo/libs/afw-components-1.0.0.tgz
3. add to node_modules
	a. npm i ./libs/afw-components-1.0.0.tgz --save-dev
npm link
npm watch 

		"build": "rollup -c",
		"build:link": "npm run build && cd dist && npm link",
		"build:pack": "npm run build && cd dist && npm pack",
		"build:new": "npm i && npm build:pack",

npm run build:pack
copy 
Could not find a declaration file for module 'afw-components'. 
'C:/Users/cliff/work/workspace-react-afw/demo/node_modules/afw-components/index.js' implicitly has an 'any' type.
  
 Try `npm i --save-dev @types/afw-components` if it exists or 
 add a new declaration (.d.ts) file containing `declare module 'afw-components';`ts(7016)
 
 
             {/* Will be opened via redux actions 'common/commonOpenModal' that is launched from the axios-intercptor handling the response and must be closed manually. */}
            <AfwModal show={showModal} dialogClassName={'danger'} closeModal={true}>
                <AfwMessage/>
            </AfwModal>
            <AfwModal show={showModal} dialogClassName={'danger'} closeModal={true}>
                <AfwMessage/>
            </AfwModal>

		"react-split-pane": "^0.1.92",

                <Modal.Header className={headerClassName} onHide={() => dispatch({ type: 'common/commonCloseModal'})} closeButton>
                    <i className={iconClassName}></i>&nbsp;
                    <h5>{status === 'danger' ? 'Error' : 'Info'}</h5>
                </Modal.Header>
